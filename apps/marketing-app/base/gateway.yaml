apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: marketing-app-gateway
  namespace: marketing-dev
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - placeholder.com
      tls:
        httpsRedirect: true

    - port:
        number: 443
        name: https
        protocol: HTTPS
      hosts:
        - placeholder.com
      tls:
        mode: SIMPLE
        credentialName: placeholder-tls