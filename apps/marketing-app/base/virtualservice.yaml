apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: marketing-app-vs
spec:
  hosts:
    - placeholder.com
  gateways:
    - marketing-app-gateway
  http:
    - route:
        - destination:
            host: marketing-app
            port:
              number: 80